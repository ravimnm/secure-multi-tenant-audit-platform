spring.application.name=agent-service

server.port=9090

smtap.audit.url=${API_GATEWAY_URL}/audit/events

agent.log.path=${AGENT_LOG_PATH}

agent.api-key=${AGENT_API_KEY}

agent.actor-id=${AGENT_ACTOR_ID}
